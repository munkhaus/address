enum LocationError {
  permissionDenied,
  serviceDisabled,
  timeout,
  unknown,
}

enum ApiError {
  networkError,
  serverError,
  searchQueryTooShort,
  noResultsFound,
  rateLimitExceeded,
  unknown,
} 